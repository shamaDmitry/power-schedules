export default function Calendar() {
  return (
    <></>
    // <div className="grid grid-cols-1 gap-4 mt-4">
    //   {Object.entries(groupData).map(([label, hours]: any) => (
    //     <div key={label} className="border p-4 bg-gray-100 rounded">
    //       <h2 className="font-bold text-lg">{label}</h2>
    //       <p className="text-sm mt-1">{hours?.join(", ")}</p>
    //     </div>
    //   ))}
    // </div>
  );
}
